{
  "name": "memory-glasses",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bash scripts/start.sh",
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "vercel-build": "tsx scripts/db-setup.ts --if-configured && pnpm build",
    "lint": "pnpm --filter web lint",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck && tsc --noEmit",
    "perception:serve": "bash scripts/serve-perception.sh",
    "db:up": "docker compose up -d --wait mongo",
    "db:down": "docker compose down",
    "db:setup": "tsx scripts/db-setup.ts",
    "db:seed": "tsx scripts/seed.ts",
    "db:sweep": "tsx scripts/db-sweep.ts",
    "bench:tts": "tsx scripts/bench-tts.ts",
    "scan:sim": "tsx scripts/scan-sim.ts"
  },
  "dependencies": {
    "@memory-glasses/db": "workspace:*",
    "@memory-glasses/shared": "workspace:*"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "3.1131.0",
    "@types/node": "^24",
    "tsx": "^4",
    "typescript": "^5"
  },
  "packageManager": "pnpm@11.5.0"
}
