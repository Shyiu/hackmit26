import { NextResponse } from "next/server";

// Filter by item and time range. See README "Data model".
export async function GET() {
  return NextResponse.json({ error: "not implemented" }, { status: 501 });
}
