import { PlaceholderPage } from "@/components/placeholder-page";

export default function ItemsPage() {
  return (
    <PlaceholderPage
      title="Items"
      description="A card per item with a thumbnail, the location sentence, and when it was last seen."
    />
  );
}
