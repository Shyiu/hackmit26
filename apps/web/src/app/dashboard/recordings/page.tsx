import { PlaceholderPage } from "@/components/placeholder-page";

export default function RecordingsPage() {
  return (
    <PlaceholderPage
      title="Recordings"
      description="Optional. Recordings that have left the headset. Until uploads exist, a recording is a file on the phone."
    />
  );
}
