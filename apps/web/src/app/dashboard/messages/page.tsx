import { PlaceholderPage } from "@/components/placeholder-page";

export default function MessagesPage() {
  return (
    <PlaceholderPage
      title="Messages"
      description="Optional. Send the wearer a short message or set a reminder. The headset shows it and the voice reads it."
    />
  );
}
