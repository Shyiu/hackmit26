import { PlaceholderPage } from "@/components/placeholder-page";

export default function RoomsPage() {
  return (
    <PlaceholderPage
      title="Rooms"
      description="Enroll a room by walking through it. Mark a room private and nothing seen there gets stored."
    />
  );
}
