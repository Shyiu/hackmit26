{
  "name": "@memory-glasses/ios",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios",
    "build:sim": "xcodebuild -project ios/App/App.xcodeproj -scheme App -configuration Debug -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath ios/DerivedData CODE_SIGNING_ALLOWED=NO build"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2",
    "typescript": "^5"
  }
}
